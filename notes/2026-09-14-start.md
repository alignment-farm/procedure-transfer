# Investigation start

Starting revision: 44c3f8c7d7e03d0d7c528f8d4a3b2013013a30e0. Read local AGENTS and README, parent research-source instructions and focused methods review, and prior acquisition protocol, results and implementation. This repository is independent of the sibling repositories.

The actual host is the 64 GiB M1 Mac Studio. Docker Model Runner client 1.2.6/server 1.2.8 exposes llama.cpp b9879-metal, revision 72874f5. Only installed model is docker.io/ai/qwen3.8:27b-q4_K_M, OCI digest sha256:f04d0a543b642a6f0d06590973b124bc4e8700ddf7e99b669ec6c4ab1ef561ef. Its upstream weight revision is not established by the inspect response. Docker's documented CLI here exposes inference, not student gradient updates. No distillation claim relies on that model or on chat logprobs.

The previous study's model files and venv are not present on this host. Installed uv 0.12.13 and created this study's own environment from the prior pinned mlx-lm Git requirement (86b48c461feebf87c58788655b7e57b5574b9e6d). Downloading the prior Qwen3-4B-Instruct-2507 revision cdbee75f17c01a7cc42f958dc650907174af0554 (approximately 8 GB). Native MLX permits a common teacher/student vocabulary, full logits and LoRA gradients; actual feasibility remains to be measured. This is a setup cost, not a reuse of already resident weights.

Provisional design: eliminate the earlier missing condition combination by covering all four combinations in the checked evidence. Compare answer-only imitation, reverse KL on checked response prefixes, and reverse KL on student-sampled prefixes, with identical acquisition inputs and teacher evidence. No extra unlabeled training cases. This separates the trajectory choice from the loss/teacher change. All conditions still assume stable routing and transformation across unseen identifiers; finite examples do not uniquely identify arbitrary programs. Native implementation will use full-vocabulary KL (a disclosed adaptation of the paper's top-256 objective). First check teacher use of evidence and loss/gradient correctness on development material; freeze the final recipe before generating evaluation material. No claim to replicate the published benchmarks or PMD's online memory co-evolution.
