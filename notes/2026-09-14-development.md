# Native feasibility and teacher development

Initial implementation committed as 0be899d. The model download completed in 741.7 seconds (Hugging Face progress timer); exact file bytes and setup details are in evidence/setup.json. All native checkpoint files covered by the previous study's hash manifest match. Environment and files are recorded in evidence/pilot-01/resource.json.

Pilot 1 completed in 71.14 seconds with 9.62 GB peak MLX allocation. The 16 development tasks scored 0/16 without evidence, 9/16 with the initial example prompt, and 16/16 with the privileged supplied rule. Tool routing was correct with examples; failures principally omitted the priority suffix, sometimes also retaining lowercase identifiers. These are development observations, not prospective transfer findings.

Six real adapter updates (two each for imitation, checked-prefix reverse KL and sampled-prefix reverse KL) produced finite, nonzero gradients. Full teacher distributions have vocabulary size 151,936. Teacher first-response distributions exactly matched independent prefix-only scoring (maximum delta 0 in all four checked comparisons). Base parameter hashes were unchanged and reset probe logits restored exactly after each method. Independent small-vector tests checked reverse-KL value, its analytic derivative, and zero teacher gradients. Training-prefix loss alignment and EOS positions are preserved in the raw update events. No pilot checkpoint or student-transfer score selected the final recipe.

One teacher-prompt revision was investigated before protocol freeze. The reminder asks for a single consistent protocol using both fields and all text-argument changes. It supplies neither the routing map nor casing nor the suffix. This is an explicit researcher-written inductive cue, not a model-generated lesson or a newly acquired correct rule. It is additional instructional context on the teacher/reference path, disclosed alongside the evidence. The predeclared selection rule in evidence/pilot-02-teacher/design.json was to select it only if it beat 9/16 on the same development cases, with no further candidates.

The reminder scored 16/16 development and 14/16 acquisition recall. The latter is a separate measurement: a teacher can mishandle even a query whose checked answer is in context. We retain these failures and proceed; teacher distributions can still be imperfect. The improved development score selects the reminder, not the student recipe. No final evaluation identifiers or outputs have been constructed. The final comparison keeps training inputs, initialization, order, optimizer, and steps fixed across treatments; its secondary contrast holds reverse KL fixed. Both teacher and explicit-evidence reference use the selected reminder verbatim.

Evidence: [pilot 1 events](../evidence/pilot-01/events.jsonl), [pilot 2 responses](../evidence/pilot-02-teacher/responses.jsonl), [pilot 2 summary](../evidence/pilot-02-teacher/summary.json). Each pilot preserves the code actually executed and file checksums. No failed native run was discarded. The only development revision was the disclosed teacher reminder; there was no learning-rate, step-count, task or student-checkpoint search.
