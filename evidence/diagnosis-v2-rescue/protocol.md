# Controlled rescue and initialization intervention

The loss-direction/rate matrix is complete. CE-high reaches 16/16 recall at 128 and 256 steps. Reverse-high reaches 4/16; reverse-low reaches 0/16 at 256. Forward-high reaches 16/16 at 128 and 14/16 at 256; forward-low reaches 14/16 at 256. The first-token probe finds eight teacher-clear but wrong decisions in reverse-high-256 with near-zero correct-token probability and negligible reverse-KL correction gradient, while the corresponding forward gradient remains large. Finite full parameter gradients did not exclude this local signal failure.

Before executing rescue: from the identical saved reverse-high-256 checkpoint, run 128 further updates under reverse KL versus forward KL, both with newly reset AdamW, lr .0005, same fixed teacher/canonical prefixes, and the first 128 indices of the diagnostic stream. Only loss direction differs between these two rescue arms. Do not select intermediate checkpoints; evaluate final recall and the existing separate development cases. This is not a fresh transfer claim. If forward restores routing/acquisition but reverse does not, it demonstrates recoverability of the same collapsed parameters and a directional-loss effect, beyond an observational gradient correlation.

Third arm: from CE-high-128, apply 128 reverse-KL steps with the same reset optimizer and stream. Compare with the matrix's reverse-high first 128 steps for an initialization intervention. CE supplied the initial acquired policy; success is not acquisition by reverse KL alone and carries the CE cost. No new labels enter any arm. Preserve all outcomes, counters and endpoint adapters. Allocation: three 128-step runs, 20-minute ceiling, 40 GB MLX peak.

Also apply the same previously specified first-token signal audit to all six accepted v1 adapters on their own original acquisition inputs and teacher context. This is retrospective diagnosis, not modification or re-scoring of the accepted prospective comparison. Add forward-high-128 to the v2 probability audit to compare the acquired checkpoint against its later 256-step endpoint. These additions were made before interpreting their probability results beyond the already observed v2 final-mode failure.
